function cloneCopyEvent(a, b) {
  var c, h, d, f, g, k, l, e;
  if (1 === b.nodeType) {
    if (dataPriv.hasData(a) && (f = dataPriv.access(a), g = dataPriv.set(b, f), e = f.events)) {
      for (d in delete g.handle, g.events = {}, e) {
        for (c = 0, h = e[d].length;c < h;c++) {
          jQuery.event.add(b, d, e[d][c]);
        }
      }
    }
    dataUser.hasData(a) && (k = dataUser.access(a), l = jQuery.extend({}, k), dataUser.set(b, l));
  }
}
;
